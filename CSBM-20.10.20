#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
	int a = 10;//4个字节
	//&a;//取地址
	int* p = &a;
	printf("%p\n", &a);//有一种变量是用来存放地址的-指针变量
	//p为一个整形指针变量
	printf("%p\n", p);
	*p=20;//结引用操作符
	printf("a=%d\n", a);

	return 0;
}
